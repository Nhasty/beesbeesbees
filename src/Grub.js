var Grub = function() {
  // 'should have an `age` property that is set to `0`
  this.age = 0;
  // should have a `color` property that is set to `pink`
  this.color = 'pink';
  // should have a `food` property that is set to `jelly`
  this.food = 'jelly';
};

// should have an `eat` method
Grub.prototype.eat = function() {

};

